# Frontend App Template

Use this template for frontend applications.

It keeps the default `knowledge/` structure and seeds prompts for routing, page state, API contracts, visual checks, and browser coverage.
