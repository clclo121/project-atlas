{
  "name": "project-kb-core",
  "version": "0.1.0",
  "description": "Git-first project knowledge base governance CLI for AI coding agents.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "project-kb": "./dist/index.js",
    "project-kb-mcp": "./dist/mcp.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/clclo121/project-kb-core.git"
  },
  "keywords": [
    "ai-coding",
    "knowledge-base",
    "opencode",
    "cli",
    "git"
  ],
  "files": [
    "dist",
    "adapters",
    "schema",
    "templates",
    "CHANGELOG.md",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint:types": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@modelcontextprotocol/server": "^2.0.0-alpha.2",
    "zod": "^4.4.3"
  }
}
